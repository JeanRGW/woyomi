# Optional self-hosted backend config (apps/server)
SYNC_TOKEN=changeme
DATA_DIR=./data
PORT=8787

# Web-mode fetch proxy (POST /api/scrape). Off by default so this server is
# not an open proxy; a self-hoster enables it and may gate it with a token.
SCRAPE_ENABLED=false
SCRAPE_TOKEN=
